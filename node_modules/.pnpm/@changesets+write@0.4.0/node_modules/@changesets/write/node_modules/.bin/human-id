#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../../human-id/dist/cli.js" "$@"
else
  exec node  "$basedir/../../../../human-id/dist/cli.js" "$@"
fi
# cmd-shim-target=/home/dkrac/axis/node_modules/.pnpm/@changesets+write@0.4.0/node_modules/human-id/dist/cli.js
